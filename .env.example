# Cal.com (headless calendar & availability)
VITE_CAL_API_KEY=your_cal_api_key_here
VITE_CAL_EVENT_TYPE_ID=your_event_type_id_here
VITE_CAL_USERNAME=redevise

# Resend (server-side only — set in Cloudflare Pages dashboard, NOT here)
# RESEND_API_KEY is consumed by functions/api/send-email.js (Cloudflare Worker)
# Add it as a Secret in: Pages > Settings > Environment Variables
